@model IEnumerable<Domain.WorkExperience>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.CompanyName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.JobTitle)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.WorkYears)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Cv)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JobTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WorkYears)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cv.CvId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WorkExperienceId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WorkExperienceId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WorkExperienceId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
